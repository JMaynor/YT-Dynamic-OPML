import os

from dotenv import load_dotenv

load_dotenv()


if __name__ == "__main__":
    """
    Main entry point of program
    """

    # First check if OPML file exists

    # If not, create it

    # Serve the OPML file
